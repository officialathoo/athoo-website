{
  "buildCommand": "pnpm --filter @workspace/athoo run build",
  "outputDirectory": "artifacts/athoo/dist",
  "installCommand": "pnpm install --frozen-lockfile=false",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
